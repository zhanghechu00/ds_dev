读取 [](file:///c%3A/program1/git/MYGPTAIv2/06-contours/extract.py#275-275)，行 275 到 325

读取 [](file:///c%3A/program1/git/MYGPTAIv2/mcp_server_shihua.py#490-490)，行 490 到 543

我检查了您的 mcp_server_shihua.py 文件，您现在已经实现的解决死锁的方法是：

在 `extract_heights_from_image` 函数的 `subprocess.Popen` 调用逻辑中：

1.  **禁用了 Python 标准输出缓冲**：
    您在构建 `cmd` 时，明确加入了 `"-u"` 参数：
    ```python
    cmd = [
        sys.executable, 
        "-u", # Unbuffered output   <-- 关键实现 1
        script_path, 
        ...
    ]
    ```

2.  **切断了标准输入 (Stdin)**：
    您在 `subprocess.Popen` 中将 `stdin` 指向了空设备，防止子进程因等待输入而挂起：
    ```python
    process = subprocess.Popen(
        cmd,
        cwd=work_dir,
        stdin=subprocess.DEVNULL, # Prevent stdin inheritance  <-- 关键实现 2
        ...
    )
    ```

3.  **增加了超时熔断机制**：
    您使用了 `process.communicate(timeout=60)`，强制设定了 60 秒的运行上限，如果超时会 kill 掉进程，防止永久死锁：
    ```python
    try:
        stdout, stderr = process.communicate(timeout=60) # 60 seconds timeout  <-- 关键实现 3
    except subprocess.TimeoutExpired:
        process.kill()
        ...
    ```

这些就是您当前代码中已经实装的防止死锁的具体措施。